import { Text, View } from 'react-native';
import React from 'react';

function Page({ navigation }) {
    return (
        <View>
            <Text>Page</Text>
        </View>
    );
}

export default Page;
