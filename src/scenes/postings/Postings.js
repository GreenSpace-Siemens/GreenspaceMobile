import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

function Postings({ navigation }) {
    return (
        <View style={styles.container}>
            <Text>Job Listings</Text>
        </View>
    );
}

const styles = StyleSheet.create({
    container: {},
});

export default Postings;
